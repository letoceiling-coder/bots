# 🔧 Исправление ошибки "Class DefStudio\Telegraph\Telegraph not found" в тестах

## Проблема

При выполнении тестов возникает ошибка:
```
Class "DefStudio\Telegraph\Telegraph" not found
```

## Причина

Пакет `defstudio/telegraph` не установлен или autoload не обновлен.

## Решение

### Шаг 1: Установите зависимости Composer

```bash
composer install
```

Или если нужно обновить только autoload:

```bash
composer dump-autoload
```

### Шаг 2: Убедитесь, что пакет установлен

Проверьте, что пакет присутствует в `vendor/`:

```bash
ls vendor/defstudio/telegraph
```

Или проверьте через composer:

```bash
composer show defstudio/telegraph
```

### Шаг 3: Очистите кеш приложения

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

### Шаг 4: Перезапустите тесты

```bash
php artisan test
```

Или напрямую через PHPUnit:

```bash
./vendor/bin/phpunit
```

## Если проблема сохраняется

### Вариант 1: Переустановите пакет

```bash
composer remove defstudio/telegraph
composer require defstudio/telegraph
composer dump-autoload
```

### Вариант 2: Проверьте версию PHP

Убедитесь, что используется PHP 8.2 или выше:

```bash
php -v
```

### Вариант 3: Проверьте composer.json

Убедитесь, что пакет указан в `require` (не в `require-dev`):

```json
"require": {
    "defstudio/telegraph": "^1.66"
}
```

### Вариант 4: Проверьте настройки тестов

Убедитесь, что в `phpunit.xml` правильно настроен bootstrap:

```xml
<phpunit bootstrap="vendor/autoload.php">
```

## Проверка после исправления

После выполнения всех шагов запустите тест:

```bash
php artisan test
```

Если ошибка исчезла, проблема решена!

## Дополнительная информация

Если проблема возникает только в определенных тестах, проверьте:

1. Используется ли `ExtendedTelegraph` в тестах напрямую
2. Загружаются ли сервисы, которые используют `ExtendedTelegraph`
3. Правильно ли настроены моки (mock) для тестов

