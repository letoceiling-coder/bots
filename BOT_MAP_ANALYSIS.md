# Анализ методов и карта бота "Центр бухгалтерского учета"

## ✅ Проверка доступных методов

### Доступные методы в BotController:

1. **sendMessage** ✅ - Отправка текстовых сообщений
2. **inlineKeyboard** ✅ - Меню с inline-кнопками (для навигации)
3. **replyKeyboard** ✅ - Reply-клавиатура
4. **question** ✅ - Вопросы пользователю
5. **sendDocument** ✅ - Отправка прайсов и документов
6. **managerChat** ✅ - Переключение на менеджера
7. **sendPhoto, sendVideo, sendAudio** ✅ - Медиа файлы
8. **sendPoll** ✅ - Опросы (не используется в описании)
9. **editMessageText** ✅ - Редактирование сообщений
10. **deleteMessage** ✅ - Удаление сообщений

### Вывод: ✅ ВСЕ НЕОБХОДИМЫЕ МЕТОДЫ ДОСТУПНЫ!

---

## 📋 Структура блоков для карты бота

### Общий формат блока:
```json
{
  "id": "string",           // Уникальный ID блока
  "type": "block",          // Тип блока
  "method": "string",       // Метод выполнения (sendMessage, inlineKeyboard и т.д.)
  "method_data": {},        // Параметры метода
  "x": number,              // Координата X для визуализации
  "y": number,              // Координата Y для визуализации
  "nextBlockId": "string",  // ID следующего блока (null для последнего или условного перехода)
  "label": "string"         // Подпись блока
}
```

---

## 🗺️ Архитектура карты

### Основные точки входа:
1. **Блок 1** - `/start` команда → Приветственное сообщение
2. **Блок 2** - Главное меню (7 пунктов)

### Основные ветки:
- **Ветка 1**: Создать/изменить/закрыть бизнес (5 подветок)
- **Ветка 2**: Бухгалтерия и отчетность (3 подветки)
- **Ветка 3**: Судебное сопровождение (4 подветки)
- **Ветка 4**: Блокировка счета (3 подветки)
- **Ветка 5**: Лицензии и сертификаты (4 подветки)
- **Ветка 6**: Тендеры и гранты (2 подветки)
- **Ветка 7**: Связаться с менеджером (простая ветка)

### Общий паттерн каждой ветки:
1. Сообщение "Отлично! Выберете нужный запрос."
2. Подменю с inline-кнопками
3. Сообщение "Ответьте на несколько вопросов..."
4. Сбор данных (ФИО, телефон, ИНН, ОПФ)
5. Специфичные вопросы (зависят от ветки)
6. Описание ситуации (опционально)
7. Согласие с политикой
8. Сообщение "Отлично! Вся информация собрана."
9. Финальные действия (4 кнопки)

### Финальные действия (общие):
- Отправить заявку → Заявка №*** зарегистрирована
- Ознакомиться с прайсом → Отправка документа
- Связаться с менеджером → Переключение на менеджера
- Главное меню → Возврат в блок 2

---

## ⚠️ Важные замечания

### Что РАБОТАЕТ:
- ✅ Создание структуры блоков
- ✅ Визуализация в диаграмме
- ✅ Сохранение в базу данных
- ✅ Тестирование блоков через API

### Что ТРЕБУЕТ ДОРАБОТКИ для полноценной работы:
- ⚠️ **Webhook handler** - для обработки `callback_query` от inline-кнопок
- ⚠️ **Message handler** - для обработки текстовых ответов пользователя
- ⚠️ **State management** - для сохранения состояния диалога (какие данные уже собраны)
- ⚠️ **Conditional routing** - для перехода к разным блокам в зависимости от выбора пользователя
- ⚠️ **Validation** - для проверки формата данных (например, ИНН - только числа)
- ⚠️ **Variables** - для подстановки имени пользователя в сообщения

### Рекомендации для реализации:
1. Создать webhook endpoint для обработки callback_query
2. Сохранять состояние диалога в Redis или БД
3. Реализовать роутинг на основе callback_data
4. Добавить валидацию для полей (ИНН и т.д.)

---

## 📊 Статистика карты

- **Общее количество блоков**: ~100-150 (оценка для полной карты)
- **Веток**: 7 основных + подветки
- **Уровней вложенности**: до 4-5 уровней
- **Типов блоков**: 5 основных (sendMessage, inlineKeyboard, question, sendDocument, managerChat)

---

## 🚀 Следующие шаги

1. ✅ Создать полную JSON карту всех блоков
2. ⏳ Реализовать webhook handler
3. ⏳ Реализовать state management
4. ⏳ Добавить валидацию
5. ⏳ Добавить поддержку переменных в тексте

