# 🔐 Настройка аутентификации GitHub

## ❌ Проблема

Вы использовали `YOUR_TOKEN` вместо реального токена. Нужно заменить его на настоящий токен.

## ✅ Решение

### Вариант 1: Использовать Personal Access Token (рекомендуется)

1. **Создайте токен на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: `Server Deploy`
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **ВАЖНО:** Скопируйте токен сразу, он показывается только один раз!

2. **Используйте токен в команде:**

```bash
# Удалить старый remote
git remote remove origin

# Добавить remote с токеном (замените YOUR_ACTUAL_TOKEN на реальный токен)
git remote add origin https://YOUR_ACTUAL_TOKEN@github.com/letoceiling-coder/bots.git

# Отправить код
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx@github.com/letoceiling-coder/bots.git
```

### Вариант 2: Использовать токен через переменную окружения

```bash
# Установить токен в переменную окружения
export GITHUB_TOKEN="your_actual_token_here"

# Удалить старый remote
git remote remove origin

# Добавить remote с использованием переменной
git remote add origin https://${GITHUB_TOKEN}@github.com/letoceiling-coder/bots.git

# Отправить код
git push -u origin main
```

### Вариант 3: Использовать SSH (более безопасно)

1. **Создайте SSH ключ на сервере:**
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов (или укажите пароль)
```

2. **Скопируйте публичный ключ:**
```bash
cat ~/.ssh/id_ed25519.pub
```

3. **Добавьте ключ в GitHub:**
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Title: `Server Deploy`
   - Key: вставьте содержимое из команды выше
   - Нажмите "Add SSH key"

4. **Используйте SSH URL:**
```bash
# Удалить старый remote
git remote remove origin

# Добавить remote через SSH
git remote add origin git@github.com:letoceiling-coder/bots.git

# Проверить подключение
ssh -T git@github.com

# Отправить код
git push -u origin main
```

## 🔍 Проверка текущего remote

```bash
# Посмотреть текущий remote
git remote -v

# Если нужно изменить URL
git remote set-url origin https://YOUR_TOKEN@github.com/letoceiling-coder/bots.git
```

## ⚠️ Важные замечания

1. **Токен должен начинаться с `ghp_`** (для классических токенов)
2. **Токен показывается только один раз** - сохраните его в безопасном месте
3. **Не коммитьте токен в Git** - он уже в `.gitignore`
4. **Используйте SSH для продакшена** - это безопаснее

## 📝 Быстрая команда (с токеном)

```bash
# Замените YOUR_ACTUAL_TOKEN на реальный токен
git remote remove origin
git remote add origin https://YOUR_ACTUAL_TOKEN@github.com/letoceiling-coder/bots.git
git push -u origin main
```

## ✅ После успешной отправки

Команда deploy будет работать:

```bash
php artisan deploy --force
```


