# 🔧 Исправление проблемы с версией PHP

## Проблема
Composer использует PHP 5.6.40 вместо PHP 8.2, который требуется для проекта.

## ⚡ Быстрое решение

### Вариант 1: Использовать php8.2 для Composer

```bash
# На сервере выполните:
php8.2 /usr/bin/composer install --no-dev --optimize-autoloader
```

Или если composer установлен локально:
```bash
php8.2 composer install --no-dev --optimize-autoloader
```

### Вариант 2: Настроить альтернативы PHP

```bash
# Установить альтернативы (если доступно)
sudo update-alternatives --set php /usr/bin/php8.2
```

### Вариант 3: Использовать полный путь к php8.2

Найдите путь к php8.2:
```bash
which php8.2
```

Затем используйте его в командах:
```bash
/usr/bin/php8.2 /usr/bin/composer install --no-dev --optimize-autoloader
```

## 📝 Обновление команды deploy

Я обновил команду `deploy`, чтобы она автоматически определяла версию PHP. Но если это не сработает, выполните вручную:

```bash
# 1. Получить обновления
git pull origin main

# 2. Обновить зависимости с правильной версией PHP
php8.2 /usr/bin/composer install --no-dev --optimize-autoloader

# 3. Обновить NPM
npm install --production

# 4. Собрать фронтенд
npm run build

# 5. Выполнить миграции
php8.2 artisan migrate --force

# 6. Очистить кэш
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 artisan route:clear
php8.2 artisan view:clear

# 7. Оптимизировать
php8.2 artisan config:cache
php8.2 artisan route:cache
php8.2 artisan view:cache
```

## 🔍 Проверка версии PHP

```bash
# Проверить текущую версию PHP
php -v

# Проверить версию php8.2
php8.2 -v

# Проверить, какой PHP использует composer
composer --version
```

## ✅ После исправления

Команда deploy должна работать:
```bash
php8.2 artisan deploy --force
```


