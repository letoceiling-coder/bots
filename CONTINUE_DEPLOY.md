# ⚡ Продолжить обновление проекта

## Текущий статус
✅ Composer зависимости установлены
✅ NPM зависимости установлены (с предупреждениями о версии Node.js)

## Следующие шаги

### 1. Попробовать собрать фронтенд

```bash
npm run build
```

Если сборка пройдет успешно - отлично! Если будут ошибки из-за версии Node.js, см. ниже.

### 2. Выполнить миграции

```bash
php8.2 artisan migrate --force
```

### 3. Очистить и оптимизировать кэш

```bash
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 artisan route:clear
php8.2 artisan view:clear

php8.2 artisan config:cache
php8.2 artisan route:cache
php8.2 artisan view:cache
```

## 🔧 Если сборка не работает из-за Node.js

### Вариант 1: Обновить Node.js через nvm

```bash
# Установить nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# Установить Node.js 20
nvm install 20
nvm use 20

# Пересобрать
npm run build
```

### Вариант 2: Использовать существующую сборку

Если на локальной машине уже есть собранные файлы в `public/build`, можно загрузить их через Git или скопировать вручную.

### Вариант 3: Собрать локально и загрузить

Соберите проект на локальной машине и закоммитьте `public/build` в Git, затем на сервере:

```bash
git pull origin main
```


