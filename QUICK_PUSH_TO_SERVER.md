# 🚀 Быстрая инструкция: Отправка на сервер

## Шаг 1: Настройка секретного ключа

### На локальной машине (в `.env`):
```env
DEPLOY_SECRET=ваш-секретный-ключ-12345
```

### На сервере (в `.env`):
```env
DEPLOY_SECRET=ваш-секретный-ключ-12345
```

**Важно**: Используйте одинаковый ключ на обеих машинах!

## Шаг 2: Использование команды

### Простое использование:
```bash
php artisan push:server
```

### С сообщением коммита:
```bash
php artisan push:server --message="Добавлена функция получения Chat ID"
```

### С указанием всех параметров:
```bash
php artisan push:server \
  --message="Обновление карты бота" \
  --branch=main \
  --server=https://parser-auto.siteaccess.ru \
  --secret=ваш-секретный-ключ
```

## Что делает команда:

1. ✅ `git add .` - Добавляет все изменения
2. ✅ `git commit -m "..."` - Создает коммит
3. ✅ `git push origin main` - Отправляет в Git
4. ✅ POST запрос на сервер - Запускает обновление

## После выполнения:

1. Дождитесь завершения обновления на сервере (обычно 1-3 минуты)
2. Обновите страницу в браузере (Ctrl+F5)
3. Проверьте работу новых функций

## 🔍 Проверка работы

### Проверить, что команда доступна:
```bash
php artisan list | grep push
```

### Тестовый запуск (без отправки на сервер):
```bash
php artisan push:server --skip-push
```

## ⚠️ Важно

- Убедитесь, что `.env` файл **НЕ** в Git (должен быть в `.gitignore`)
- Секретный ключ должен быть одинаковым на локальной машине и сервере
- После изменения `.env` на сервере выполните: `php artisan config:clear`

Готово! 🎉

