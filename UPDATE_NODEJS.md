# 🔧 Обновление Node.js на сервере

## Проблема
Node.js v17.9.1 слишком старая версия. Vite 7.2.4 требует Node.js 20.19+ или 22.12+.

## ⚡ Решение: Установить Node.js через nvm

### Шаг 1: Установить nvm (если не установлен)

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

### Шаг 2: Загрузить nvm в текущую сессию

```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```

Или перезайти в терминал.

### Шаг 3: Установить Node.js 20

```bash
nvm install 20
nvm use 20
nvm alias default 20
```

### Шаг 4: Проверить версию

```bash
node -v
npm -v
```

Должно показать версию 20.x.x

### Шаг 5: Пересобрать проект

```bash
# Переустановить зависимости с новой версией Node.js
npm install

# Собрать фронтенд
npm run build
```

## 📝 Альтернатива: Использовать готовую сборку

Если обновление Node.js невозможно, можно собрать проект локально и загрузить:

### На локальной машине:

```bash
npm run build
git add public/build
git commit -m "Add built assets"
git push origin main
```

### На сервере:

```bash
git pull origin main
```

## 🔄 Автоматическая установка nvm (полный скрипт)

```bash
# Установить nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Загрузить nvm
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Установить Node.js 20
nvm install 20
nvm use 20
nvm alias default 20

# Проверить
node -v

# Переустановить зависимости
npm install

# Собрать
npm run build
```

## ⚠️ Важно

После установки nvm добавьте в `~/.bashrc` или `~/.bash_profile`:

```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```

Чтобы nvm загружался автоматически при входе в систему.


