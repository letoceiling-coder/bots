# 🔧 Решение ошибки "не найден git репозиторий"

## ⚡ Быстрое решение

Выполните на сервере следующие команды:

```bash
# 1. Перейти в директорию проекта
cd ~/parser-auto.site-access.ru/public_html

# 2. Инициализировать Git
git init

# 3. Исправить проблему с правами доступа (если возникает ошибка "dubious ownership")
git config --global --add safe.directory $(pwd)

# Или для конкретной директории:
git config --global --add safe.directory /home/d/dsc23ytp/parser-auto.site-access.ru/public_html

# 4. Переименовать ветку master в main
git branch -m master main

# 5. Добавить все файлы
git add .

# 4. Создать первый commit
git commit -m "Initial commit from server"

# 5. Добавить remote (замените YOUR_TOKEN на ваш GitHub токен)
git remote add origin https://YOUR_TOKEN@github.com/letoceiling-coder/bots.git

# Или если используете SSH:
# git remote add origin git@github.com:letoceiling-coder/bots.git

# 6. Настроить ветку
git branch -M main

# 7. Получить изменения с GitHub (если репозиторий не пустой)
git pull origin main --allow-unrelated-histories

# 8. Отправить изменения (если нужно)
git push -u origin main
```

## 🔐 Получение GitHub токена

1. Перейдите на GitHub: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. Используйте его в команде выше вместо `YOUR_TOKEN`

## ✅ После настройки

Теперь команда deploy будет работать:

```bash
php artisan deploy --force
```

## 📝 Альтернатива: Использовать скрипт

Если скрипт `setup-git.sh` доступен на сервере:

```bash
bash setup-git.sh
```

Скрипт проведет вас через весь процесс настройки.

## 🐛 Если возникают проблемы

### Ошибка "Authentication failed"

Используйте токен в URL:
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/letoceiling-coder/bots.git
```

### Ошибка "Repository not found"

- Проверьте правильность URL репозитория
- Убедитесь, что токен имеет права `repo`
- Проверьте, что репозиторий существует и доступен

### Ошибка "Updates were rejected"

```bash
# Получить последние изменения
git fetch origin
git pull origin main --allow-unrelated-histories

# Отправить изменения
git push -u origin main
```

## 📚 Подробная инструкция

Смотрите файл `SETUP_GIT.md` для детальной информации.

