# Инструкция: Отправка изменений на сервер

## 📋 Описание

Команда `php artisan push:server` автоматически:
1. ✅ Добавляет изменения в Git
2. ✅ Создает коммит
3. ✅ Отправляет изменения в Git репозиторий
4. ✅ Отправляет запрос на сервер для автоматического обновления

## 🚀 Быстрый старт

### 1. Настройка секретного ключа

#### На локальной машине (в `.env`):
```env
DEPLOY_SECRET=your-secret-key-here-12345
```

#### На сервере (в `.env`):
```env
DEPLOY_SECRET=your-secret-key-here-12345
```

**Важно**: Используйте одинаковый секретный ключ на локальной машине и сервере!

### 2. Использование команды

```bash
php artisan push:server
```

Команда автоматически:
- Создаст коммит с сообщением "Update from local development"
- Отправит изменения в ветку `main`
- Отправит запрос на обновление на сервер `https://parser-auto.siteaccess.ru`

## 📝 Опции команды

### Основные опции

```bash
# Указать сообщение для коммита
php artisan push:server --message="Исправлена ошибка в карте бота"

# Указать ветку
php artisan push:server --branch=develop

# Указать URL сервера
php artisan push:server --server=https://parser-auto.siteaccess.ru

# Указать секретный ключ
php artisan push:server --secret=your-secret-key

# Пропустить коммит (только push существующих коммитов)
php artisan push:server --skip-commit

# Пропустить push (только коммит, без отправки)
php artisan push:server --skip-push
```

### Комбинированные опции

```bash
# Полный пример с всеми опциями
php artisan push:server \
  --message="Добавлена функция получения Chat ID" \
  --branch=main \
  --server=https://parser-auto.siteaccess.ru \
  --secret=your-secret-key-12345
```

## 🔐 Безопасность

### Настройка секретного ключа

1. **Сгенерируйте безопасный ключ:**
   ```bash
   # Linux/Mac
   openssl rand -hex 32
   
   # Или используйте онлайн генератор
   ```

2. **Добавьте в `.env` на локальной машине:**
   ```env
   DEPLOY_SECRET=ваш-секретный-ключ-здесь
   ```

3. **Добавьте в `.env` на сервере:**
   ```env
   DEPLOY_SECRET=ваш-секретный-ключ-здесь
   ```

4. **Проверьте, что `.env` в `.gitignore`** (не должен попадать в Git!)

### Защита роута

Роут `/api/deploy` защищен:
- ✅ Проверкой секретного ключа
- ✅ Rate limiting (10 запросов в минуту)
- ✅ Логированием всех попыток доступа

## 📡 API Endpoint на сервере

### POST `/api/deploy`

**Запрос:**
```json
{
  "secret": "your-secret-key-12345",
  "branch": "main",
  "timestamp": "2025-11-23T16:00:00Z"
}
```

**Ответ (успех):**
```json
{
  "message": "Обновление запущено",
  "status": "running",
  "branch": "main",
  "pid": 12345
}
```

**Ответ (ошибка):**
```json
{
  "message": "Неверный секретный ключ",
  "error": "Доступ запрещен"
}
```

## 🔄 Процесс обновления

1. **Локальная машина:**
   ```bash
   php artisan push:server
   ```
   - Git add, commit, push
   - POST запрос на сервер

2. **Сервер:**
   - Получает запрос
   - Проверяет секретный ключ
   - Запускает `php artisan deploy --force`
   - Обновляет проект из Git

## 🐛 Решение проблем

### Ошибка: "Git репозиторий не найден"

```bash
# Инициализируйте репозиторий
git init
git remote add origin https://github.com/your-repo.git
```

### Ошибка: "Секретный ключ не указан"

```bash
# Добавьте в .env
DEPLOY_SECRET=your-secret-key

# Или используйте опцию
php artisan push:server --secret=your-secret-key
```

### Ошибка: "Неверный секретный ключ"

- Проверьте, что ключ одинаковый на локальной машине и сервере
- Убедитесь, что `.env` файл обновлен на сервере
- Выполните `php artisan config:clear` на сервере

### Ошибка: "Сервер недоступен"

- Проверьте URL сервера
- Проверьте доступность сервера: `curl https://parser-auto.siteaccess.ru/api/deploy`
- Проверьте настройки firewall

### Ошибка при push в Git

```bash
# Проверьте настройки Git
git remote -v
git config --list

# Проверьте доступ к репозиторию
git fetch origin
```

## 📊 Логирование

Все запросы на обновление логируются на сервере:

```bash
# Просмотр логов
tail -f storage/logs/laravel.log | grep deploy
```

## 🔄 Альтернативный способ (без команды)

Если команда не работает, можно выполнить вручную:

```bash
# 1. Git операции
git add .
git commit -m "Update from local"
git push origin main

# 2. Отправка запроса на сервер
curl -X POST https://parser-auto.siteaccess.ru/api/deploy \
  -H "Content-Type: application/json" \
  -d '{
    "secret": "your-secret-key",
    "branch": "main"
  }'
```

## ⚙️ Настройка на сервере

### Проверка работы роута

```bash
# Тестовый запрос (должен вернуть ошибку без секрета)
curl -X POST https://parser-auto.siteaccess.ru/api/deploy \
  -H "Content-Type: application/json" \
  -d '{"branch": "main"}'
```

### Проверка секретного ключа

```bash
# На сервере
php artisan tinker
>>> config('app.deploy_secret')
```

## 📝 Примеры использования

### Ежедневная разработка

```bash
# Простое обновление
php artisan push:server --message="Добавлена новая функция"
```

### Разработка в отдельной ветке

```bash
php artisan push:server \
  --branch=feature/new-feature \
  --message="Новая функция: карта бота"
```

### Быстрое исправление

```bash
php artisan push:server \
  --message="Hotfix: исправлена критическая ошибка" \
  --skip-commit  # Если изменения уже закоммичены
```

## 🎯 Рекомендации

1. **Используйте описательные сообщения коммитов**
2. **Проверяйте изменения перед отправкой** (`git status`, `git diff`)
3. **Используйте отдельные ветки для больших изменений**
4. **Регулярно обновляйте секретный ключ**
5. **Мониторьте логи на сервере**

## 🔒 Безопасность

⚠️ **Важно:**

1. Никогда не коммитьте `.env` файл с секретным ключом
2. Используйте сложный секретный ключ (минимум 32 символа)
3. Регулярно меняйте секретный ключ
4. Ограничьте доступ к серверу по IP (если возможно)
5. Используйте HTTPS для всех запросов

Готово! 🎉

