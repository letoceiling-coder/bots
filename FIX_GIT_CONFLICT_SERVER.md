# 🔧 Исправление конфликта Git на сервере

## Проблема
Локальные изменения на сервере конфликтуют с обновлениями из Git.

## Решение

Выполните на сервере:

```bash
cd /home/d/dsc23ytp/parser-auto.site-access.ru/public_html

# Сохранить локальные изменения в stash
git stash push -u -m "Local changes before pull: $(date +%Y-%m-%d_%H-%M-%S)"

# Получить обновления
git pull origin main

# Очистить кеш
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 artisan route:clear
php8.2 artisan view:clear
```

## Альтернатива: Использовать команду deploy

Команда `deploy` автоматически сохраняет изменения в stash:

```bash
php8.2 artisan deploy --force
```

## Если нужно восстановить сохраненные изменения

```bash
# Посмотреть список сохраненных изменений
git stash list

# Восстановить последние изменения
git stash pop
```

