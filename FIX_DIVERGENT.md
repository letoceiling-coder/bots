# 🔀 Исправление ошибки "divergent branches"

## ⚡ Быстрое решение

Выполните на сервере:

```bash
# 1. Настроить стратегию объединения (merge)
git config pull.rebase false

# 2. Повторить pull с объединением
git pull origin main --allow-unrelated-histories

# 3. Если все прошло, отправить изменения
git push -u origin main
```

## 📝 Альтернатива: указать стратегию прямо в команде

```bash
# Объединить через merge
git pull origin main --allow-unrelated-histories --no-rebase

# Или через rebase (более чистая история)
git pull origin main --allow-unrelated-histories --rebase
```

## ✅ Полная последовательность команд

```bash
# 1. Настроить merge как стратегию по умолчанию
git config pull.rebase false

# 2. Получить и объединить изменения
git pull origin main --allow-unrelated-histories

# 3. Если были конфликты, разрешите их и выполните:
# git add .
# git commit -m "Merge server and GitHub histories"

# 4. Отправить объединенную версию
git push -u origin main
```


