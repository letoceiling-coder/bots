# 🔧 Проблема с версией Node.js

## Текущая ситуация
- Установлен Node.js v17.9.1
- Требуется Node.js >=18 или >=20 для некоторых пакетов
- Установка прошла успешно, но могут быть проблемы при сборке

## ⚡ Быстрое решение

### Вариант 1: Попробовать собрать (может сработать)

```bash
npm run build
```

Если сборка пройдет успешно - отлично! Если будут ошибки, переходите к варианту 2.

### Вариант 2: Обновить Node.js

#### Использовать nvm (если установлен):

```bash
# Установить nvm (если не установлен)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# Установить Node.js 20
nvm install 20
nvm use 20

# Проверить версию
node -v

# Теперь собрать
npm install --production
npm run build
```

#### Использовать альтернативную версию Node.js (если доступна):

```bash
# Проверить доступные версии
which node18
which node20

# Если доступна, использовать:
node20 --version
```

#### Обновить через пакетный менеджер (требует sudo):

```bash
# Для Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверить версию
node -v
npm -v
```

## 📝 Продолжить обновление проекта

После решения проблемы с Node.js:

```bash
# 1. Обновить зависимости (если нужно)
npm install --production

# 2. Собрать фронтенд
npm run build

# 3. Выполнить миграции
php8.2 artisan migrate --force

# 4. Очистить кэш
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 artisan route:clear
php8.2 artisan view:clear

# 5. Оптимизировать
php8.2 artisan config:cache
php8.2 artisan route:cache
php8.2 artisan view:cache
```

## ⚠️ Если сборка не работает

Можно попробовать собрать с игнорированием версии:

```bash
npm run build -- --ignore-engines
```

Или временно обновить package.json, чтобы убрать проверку версий (не рекомендуется для продакшена).

