# 🔧 Установка Telegraph на сервере

## Проблема
Пакет `defstudio/telegraph` не установлен на сервере.

## Решение

Выполните на сервере следующие команды:

```bash
cd /home/d/dsc23ytp/parser-auto.site-access.ru/public_html

# Установить все зависимости
composer install --no-dev --optimize-autoloader

# Или если нужно обновить только этот пакет
composer update defstudio/telegraph --no-dev

# Очистить кеш
php8.2 artisan config:clear
php8.2 artisan cache:clear
composer dump-autoload
```

## Проверка после установки

```bash
composer show defstudio/telegraph
```

Должен быть виден пакет `defstudio/telegraph v1.66.0`.

## Если composer install не работает

Проверьте:
1. Что `composer.json` содержит пакет
2. Что `composer.lock` обновлен
3. Выполните `git pull origin main` чтобы получить последние изменения

## Альтернатива: Запустить deploy снова

```bash
php8.2 artisan deploy --force
```

Это установит все зависимости автоматически.

