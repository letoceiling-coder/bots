# Настройка Git на сервере

Если на сервере проект не был клонирован из Git, нужно инициализировать репозиторий.

## 🚀 Быстрая настройка

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
# Скачайте скрипт на сервер или создайте его
bash setup-git.sh
```

Скрипт автоматически:
- Инициализирует Git репозиторий
- Добавит все файлы
- Настроит подключение к GitHub
- Проверит соединение

### Вариант 2: Ручная настройка

```bash
# 1. Перейти в директорию проекта
cd /path/to/your/project

# 2. Инициализировать Git
git init

# 3. Добавить все файлы
git add .

# 4. Создать первый commit
git commit -m "Initial commit from server"

# 5. Добавить remote
git remote add origin https://github.com/letoceiling-coder/bots.git

# 6. Настроить ветку
git branch -M main

# 7. Проверить подключение
git fetch origin
```

## 🔐 Настройка аутентификации

### Способ 1: HTTPS с Personal Access Token (рекомендуется для начала)

1. Создайте Personal Access Token на GitHub:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token
   - Выберите права: `repo`

2. Используйте токен вместо пароля:
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/letoceiling-coder/bots.git
```

Или при первом push введите токен как пароль.

### Способ 2: SSH ключи (более безопасно)

1. Создайте SSH ключ на сервере:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. Скопируйте публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub
```

3. Добавьте ключ в GitHub:
   - Settings → SSH and GPG keys → New SSH key
   - Вставьте публичный ключ

4. Измените URL на SSH:
```bash
git remote set-url origin git@github.com:letoceiling-coder/bots.git
```

5. Проверьте подключение:
```bash
ssh -T git@github.com
```

## 📥 Синхронизация с GitHub

### Если репозиторий на GitHub пустой:

```bash
git push -u origin main
```

### Если репозиторий на GitHub уже содержит код:

```bash
# Вариант 1: Merge (сохраняет обе истории)
git pull origin main --allow-unrelated-histories

# Вариант 2: Rebase (более чистая история)
git pull origin main --rebase

# После синхронизации отправьте изменения
git push -u origin main
```

## ✅ Проверка настройки

После настройки проверьте:

```bash
# Проверить remote
git remote -v

# Проверить статус
git status

# Проверить подключение
git fetch origin
```

## 🔄 После настройки

Теперь можно использовать команду deploy:

```bash
php artisan deploy --force
```

## 🐛 Устранение проблем

### Ошибка "Permission denied"

```bash
# Проверить права на .git директорию
ls -la .git

# Установить права
chmod -R 755 .git
```

### Ошибка "Authentication failed"

- Проверьте правильность токена/пароля
- Для SSH проверьте ключ: `ssh -T git@github.com`
- Убедитесь, что ключ добавлен в GitHub

### Ошибка "Repository not found"

- Проверьте правильность URL репозитория
- Убедитесь, что у вас есть доступ к репозиторию
- Проверьте имя пользователя и название репозитория

### Ошибка "Updates were rejected"

```bash
# Получить последние изменения
git fetch origin

# Слить изменения
git pull origin main --rebase

# Отправить изменения
git push origin main
```

## 📝 Пример полной настройки

```bash
# 1. Перейти в директорию проекта
cd ~/parser-auto.site-access.ru/public_html

# 2. Инициализировать Git
git init

# 3. Добавить файлы
git add .

# 4. Создать commit
git commit -m "Initial commit from server"

# 5. Добавить remote (с токеном)
git remote add origin https://YOUR_TOKEN@github.com/letoceiling-coder/bots.git

# 6. Настроить ветку
git branch -M main

# 7. Получить изменения с GitHub (если есть)
git pull origin main --allow-unrelated-histories

# 8. Отправить изменения (если нужно)
git push -u origin main

# 9. Теперь можно использовать deploy
php artisan deploy --force
```


