# 🔧 Исправление ошибки "vite: not found"

## Проблема
Vite не найден, потому что он находится в devDependencies, а установка была с флагом `--production`.

## ⚡ Решение

### Вариант 1: Установить все зависимости (включая dev)

```bash
# Установить все зависимости (включая dev для сборки)
npm install

# Затем собрать
npm run build
```

### Вариант 2: Установить только dev-зависимости

```bash
# Установить только dev-зависимости
npm install --only=dev

# Или
npm install --include=dev

# Затем собрать
npm run build
```

## 📝 Полная последовательность

```bash
# 1. Установить все зависимости (включая dev)
npm install

# 2. Собрать фронтенд
npm run build

# 3. Выполнить миграции
php8.2 artisan migrate --force

# 4. Очистить кэш
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 artisan route:clear
php8.2 artisan view:clear

# 5. Оптимизировать
php8.2 artisan config:cache
php8.2 artisan route:cache
php8.2 artisan view:cache
```

## ⚠️ Примечание

Для сборки проекта нужны dev-зависимости (vite, tailwindcss и др.). После сборки они не нужны для работы приложения, но необходимы для процесса сборки.

## 🔄 Обновление команды deploy

В будущем команда deploy должна устанавливать все зависимости для сборки, а затем можно удалить dev-зависимости после сборки (опционально).

