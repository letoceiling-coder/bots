# 🔧 Исправление: Использование правильной версии PHP для Composer

## Проблема
На сервере используется PHP 5.6.40, а проект требует PHP 8.2.

## Решение

Используйте `php8.2` для выполнения composer команд:

```bash
cd /home/d/dsc23ytp/parser-auto.site-access.ru/public_html

# Определите путь к composer
which composer
# или
/home/d/dsc23ytp/.local/bin/composer

# Установите зависимости с правильной версией PHP
php8.2 /home/d/dsc23ytp/.local/bin/composer install --no-dev --optimize-autoloader
```

Или если composer доступен через PATH:

```bash
php8.2 $(which composer) install --no-dev --optimize-autoloader
```

## Проверка версии PHP

Проверьте, какая версия PHP используется:

```bash
php -v          # Должна быть 5.6.40 (старая)
php8.2 -v       # Должна быть 8.2.x (правильная)
```

## Альтернатива: Использовать команду deploy

Команда `deploy` автоматически использует правильную версию PHP:

```bash
php8.2 artisan deploy --force
```

Это установит все зависимости с правильной версией PHP.

## После установки

Очистите кеш:

```bash
php8.2 artisan config:clear
php8.2 artisan cache:clear
php8.2 /home/d/dsc23ytp/.local/bin/composer dump-autoload
```

## Проверка

После установки проверьте:

```bash
php8.2 /home/d/dsc23ytp/.local/bin/composer show defstudio/telegraph
```

Должен быть виден пакет `defstudio/telegraph v1.66.0`.

