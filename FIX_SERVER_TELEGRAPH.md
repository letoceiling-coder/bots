# 🔧 Исправление ошибки "Class DefStudio\Telegraph\Telegraph not found" на сервере

## Проблема

На сервере возникает ошибка:
```
Class "DefStudio\Telegraph\Telegraph" not found
```

## Причина

Пакет `defstudio/telegraph` не установлен на сервере. Это происходит потому, что:
1. Пакет был добавлен в `composer.json`, но не был установлен на сервере
2. Команда `deploy` должна устанавливать зависимости, но возможно что-то пошло не так

## Решение

### Вариант 1: Установить зависимости вручную на сервере

Выполните на сервере:

```bash
cd /home/d/dsc23ytp/parser-auto.site-access.ru/public_html
composer install
```

Или если нужно обновить только этот пакет:

```bash
composer update defstudio/telegraph
```

### Вариант 2: Использовать команду deploy

Команда `deploy` должна автоматически устанавливать зависимости. Выполните:

```bash
php8.2 artisan deploy --force
```

### Вариант 3: Проверить и исправить вручную

1. Проверьте, что пакет есть в `composer.json`:
   ```bash
   grep -i telegraph composer.json
   ```

2. Убедитесь, что `composer.lock` обновлен:
   ```bash
   git pull origin main
   composer install
   ```

3. Очистите кеш:
   ```bash
   php8.2 artisan config:clear
   php8.2 artisan cache:clear
   composer dump-autoload
   ```

## Проверка после исправления

После установки пакета проверьте:

```bash
composer show defstudio/telegraph
```

Должен быть виден пакет с версией.

## Автоматическое исправление

Команда `deploy` уже включает установку Composer зависимостей. Если проблема сохраняется:

1. Убедитесь, что `composer.lock` обновлен в Git
2. Выполните `php artisan push:server` локально
3. На сервере выполните `php artisan deploy --force`

Это должно автоматически установить все зависимости, включая `defstudio/telegraph`.

